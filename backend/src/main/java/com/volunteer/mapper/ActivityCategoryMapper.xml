<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.volunteer.mapper.ActivityCategoryMapper">
    <select id="selectAll" resultType="com.volunteer.entity.ActivityCategory">
        SELECT id, name, description, create_time as createTime FROM activity_category ORDER BY id DESC
    </select>
    <select id="selectById" resultType="com.volunteer.entity.ActivityCategory">
        SELECT id, name, description, create_time as createTime FROM activity_category WHERE id = #{id}
    </select>
    <insert id="insert">
        INSERT INTO activity_category (name, description) VALUES (#{name}, #{description})
    </insert>
    <update id="update">
        UPDATE activity_category SET name = #{name}, description = #{description} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM activity_category WHERE id = #{id}
    </delete>
</mapper>